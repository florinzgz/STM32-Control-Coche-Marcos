# ESP32-S3 HMI — Source Directory

This directory contains C++ source files for the ESP32-S3 HMI firmware.

The `main.cpp` entry point and all module implementations will be added
after the design document (`docs/ESP32_FIRMWARE_DESIGN.md`) is validated.

**Framework:** Arduino only — no ESP-IDF APIs.
